# 重组生成功能说明

## 功能概述

重组生成功能是两轮对比生成系统的一个重要增强，它提供了一种全新的第二轮号码生成策略：**仅从第一轮生成的号码中重新组合**，而不引入任何新的号码。

## 核心特点

### 🎯 集中选号策略
- 第二轮号码完全来自第一轮生成的号码池
- 提供更加集中和聚焦的选号方案
- 避免号码范围过于分散

### 🔄 智能重组算法
- 5种不同的重组策略，确保多样性
- 基于频率、模式、平衡等多维度考虑
- 支持变化强度调节（0.0-1.0）

### 📊 完整分析支持
- 显示号码池信息（前区/后区可用号码）
- 标注每组号码的重组策略
- 提供详细的对比分析

## 使用方法

### 1. 启用重组模式
在两轮对比生成界面中：
1. 调整"第二轮变化强度"滑块
2. 勾选"🔄 启用重组模式"复选框
3. 点击"🔄 两轮对比生成"按钮

### 2. 查看结果
- **第一轮结果**：正常的预测号码
- **第二轮结果**：仅从第一轮号码重新组合的结果
- **对比分析**：两轮号码的重叠和差异分析

## 重组策略详解

### 1. 基于频率的重组 (frequency_based)
- 优先选择第一轮中出现频率高的号码
- 适合保守型选号策略

### 2. 保持模式的重组 (pattern_preserved)
- 尽量保持第一轮的奇偶、大小分布模式
- 在保持规律的基础上进行变化

### 3. 平衡混合重组 (balanced_mix)
- 平衡考虑频率和随机性
- 提供中等风险的选号方案

### 4. 多样化选择重组 (diverse_selection)
- 降低高频号码权重，增加多样性
- 适合追求差异化的选号策略

### 5. 边缘探索重组 (edge_exploration)
- 提高低频号码权重
- 探索第一轮中较少使用的号码组合

## 变化强度说明

- **0.0-0.3**：保守重组，主要基于第一轮的热门号码
- **0.4-0.6**：平衡重组，兼顾热门和冷门号码
- **0.7-1.0**：激进重组，更多随机性和探索性

## 适用场景

### ✅ 推荐使用重组模式的情况：
1. 对第一轮生成结果比较满意
2. 希望在有限范围内寻找更多组合
3. 追求集中化的选号策略
4. 想要避免号码过于分散

### ⚠️ 建议使用传统模式的情况：
1. 希望获得更大范围的号码覆盖
2. 第一轮号码池较小（前区<10个，后区<4个）
3. 追求最大化的号码多样性

## 技术实现

### 核心算法
```python
def generate_recombined_numbers(first_round_candidates, count, variation_strength):
    # 1. 提取第一轮所有号码构建号码池
    # 2. 分析第一轮号码特征和模式
    # 3. 应用5种不同的重组策略
    # 4. 根据变化强度调整选择权重
    # 5. 生成指定数量的重组号码
```

### 质量保证
- 严格验证所有生成号码都来自第一轮
- 避免重复组合的生成
- 确保每种策略的独特性

## 使用建议

### 最佳实践
1. **先观察第一轮结果**：确认号码池是否足够丰富
2. **选择合适的变化强度**：根据风险偏好调整
3. **对比两种模式**：可以同时尝试传统模式和重组模式
4. **关注重组策略**：不同策略适合不同的选号偏好

### 注意事项
- 重组模式要求第一轮至少生成5个不同的前区号码和2个不同的后区号码
- 如果第一轮号码池过小，系统会自动提示并可能回退到传统模式
- 重组结果的多样性取决于第一轮号码的丰富程度

## 示例对比

### 第一轮生成结果：
```
候选1: 前区[7, 14, 21, 28, 35], 后区[6, 12]
候选2: 前区[1, 8, 15, 22, 29], 后区[2, 8]  
候选3: 前区[3, 10, 17, 24, 31], 后区[4, 10]
```

### 号码池：
- 前区：[1, 3, 7, 8, 10, 14, 15, 17, 21, 22, 24, 28, 29, 31, 35] (15个)
- 后区：[2, 4, 6, 8, 10, 12] (6个)

### 重组结果示例：
```
重组1 (频率优先): 前区[8, 14, 21, 28, 31], 后区[8, 12]
重组2 (模式保持): 前区[1, 10, 17, 22, 35], 后区[2, 6]
重组3 (平衡混合): 前区[3, 15, 21, 24, 29], 后区[4, 10]
```

## 总结

重组生成功能为用户提供了一种全新的选号思路，通过在有限的号码范围内进行智能重组，既保持了与第一轮结果的关联性，又提供了足够的变化和多样性。这种方法特别适合那些对初步预测结果比较认可，但希望在此基础上探索更多可能性的用户。