# 连号功能修复说明

## 问题描述
用户反映在设置"最大前区连号数为1"的情况下，生成的号码连号概率太大，甚至会有多个连号。

## 问题原因
原始代码中的连号检查逻辑有以下问题：

1. **概念混淆**：原代码使用`consecutive_pairs_count`函数计算"连号对数"，但用户期望的是"连号组数"
   - **连号对数**：相邻连续数字的对数，如[1,2,3,5,6]有3对：(1,2), (2,3), (5,6)
   - **连号组数**：连续号码段的数量，如[1,2,3,5,6]有2组：[1,2,3]和[5,6]

2. **函数结构错误**：`consecutive_groups_count`函数被错误地放在`gen_numbers`函数内部，导致整个生成器无法正常工作

## 修复内容

### 1. 添加了正确的连号组数计算函数
```python
def consecutive_groups_count(nums: List[int]) -> int:
    """计算连号组数（连续号码段的数量）"""
    if len(nums) < 2:
        return 0
    
    groups = 0
    in_group = False
    
    for i in range(1, len(nums)):
        if nums[i] == nums[i-1] + 1:
            if not in_group:
                groups += 1
                in_group = True
        else:
            in_group = False
    
    return groups
```

### 2. 增强了连号检查逻辑
- 支持选择检查"连号对数"或"连号组数"
- 支持"等于"、"至少"、"最多"三种匹配方式
- 添加了清晰的用户界面说明

### 3. 更新了用户界面
在Tab3（号码生成）和Tab4（未来预测）中：
- 将"前区连号数量(对数)"改为"前区连号限制数量"
- 添加了"连号匹配方式"选择：等于、至少、最多
- 添加了"连号检查类型"选择：连号组数、连号对数
- 添加了详细的说明文字

### 4. 更新了策略管理
- 策略保存和加载功能现在包含新的连号参数
- 支持向后兼容旧的策略文件

### 5. 修复了生成器结构问题
- 将辅助函数移到模块级别
- 修复了函数缩进问题
- 确保生成器能正常返回结果

## 使用说明

### 推荐设置（解决用户问题）
- **连号限制数量**：1
- **连号匹配方式**：最多
- **连号检查类型**：连号组数

这样设置意味着：最多允许1组连号，比如[1,2]或[5,6,7]算作1组。

### 其他设置示例
1. **完全无连号**：
   - 连号限制数量：0
   - 连号匹配方式：最多
   - 连号检查类型：连号组数

2. **允许多组短连号**：
   - 连号限制数量：1
   - 连号匹配方式：最多
   - 连号检查类型：连号对数

## 测试验证
修复后的功能已通过以下测试：
- ✅ 0组连号生成测试
- ✅ 最多1组连号生成测试
- ✅ 连号对数vs连号组数对比测试
- ✅ 各种匹配方式测试

## 影响范围
- `backend/generator.py`：核心生成逻辑
- `backend/optimizer.py`：策略参数类
- `app.py`：用户界面和参数处理
- 所有使用号码生成功能的模块

修复完成后，用户设置"最大前区连号数为1"时，生成的号码将严格遵守最多1组连号的限制。